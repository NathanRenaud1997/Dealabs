{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="jumbotron">


        <h1>Login</h1>
        <form action="{{ path('secu_login') }}" method="post">
            <div class="form-group">
                <input type="text" name="_username" class="form-control" placeholder="Nom d'utilisateur" >
            </div>
            <div class="form-group">
                <input type="password" name="_password" class="form-control" required placeholder="Mot de passe">
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-success">Connexion</button>
            </div>

        </form>
    </div>
        <a class="btn btn-primary" href="{{ path('user_new') }}">Créer un compte</a>
    </div>
{% endblock %}